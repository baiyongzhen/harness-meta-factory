# Harness Eval 기술 검증 환경 — Python 패키지 (경량)
# docs/102.Eval 가이드 (01~04) 기준
#
# 의도적으로 제외 (TensorFlow / PyTorch 전이 의존성):
#   - bfcl-eval     → torch
#   - evals         → tensorflow
# skillopt 기본 설치는 torch/tensorflow 없음 (extras: qwen→vllm, alfworld 등은 설치하지 않음)

# 예시 하네스·공통 테스트
pytest>=8.3,<9
pytest-asyncio>=0.23,<1
pytest-cov>=5.0,<6
anyio>=4.4,<5
httpx>=0.27,<1
pyyaml>=6.0,<7
rich>=13.0,<14
numpy>=1.24,<3
openpyxl>=3.1,<4

# 01. 스킬·하네스 품질 Eval
skillopt>=0.1.0
# 로컬 Ollama 연동 (SkillOpt openai_compatible + 헬스체크/모델 조회)
ollama>=0.4,<1
pydantic>=2.9,<3

# 02. 코딩 에이전트 Harness Eval
swebench>=4.0,<5
sb-cli>=0.1.0

# 03. 범용 에이전트·도구 Eval (경량)
agentevals>=0.0.9
datasets>=2.14,<4
huggingface_hub>=0.20,<1

# 공통 API·도구
openai>=1.40,<2
anthropic>=0.34,<1
azure-core>=1.30,<2
azure-identity>=1.15,<2
tiktoken>=0.7,<1
tenacity>=8.2,<10
docker>=7.0,<8
unidiff>=0.7,<1
